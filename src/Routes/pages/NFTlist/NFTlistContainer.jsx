import NFTlistPresenter from './NFTlistPresenter';

const NFTlistContainer = (props) => {
  /* Router */
  /* State */
  const {} = props;
  /* Hooks */
  /* Functions */
  /* Render */
  return <NFTlistPresenter />;
};

export default NFTlistContainer;
