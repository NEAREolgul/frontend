import Inspiration from '../../../Components/Inspiration';

const NFTlistPresenter = (props) => {
  /* Router */
  /* State */
  const {} = props;
  /* Hooks */
  /* Functions */
  /* Render */
  return <Inspiration List={true} />;
};

export default NFTlistPresenter;
